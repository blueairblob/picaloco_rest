// package.json
{
  "name": "picaloco-api-docs",
  "version": "1.0.0",
  "description": "Pica Loco API Documentation hosted on Vercel",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "deploy": "vercel"
  },
  "dependencies": {
    "swagger-ui-express": "^5.0.0",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": "18.x"
  }
}